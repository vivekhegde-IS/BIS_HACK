services:
  - type: web
    name: bis-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.app:app
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
